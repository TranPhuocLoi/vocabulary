<section class="bg-warning text-white py-5 mb-5">
  <div class="container text-center">
    <h1>VOCABULARY LIBRARY</h1>
    <p class="lead">Starting points for finding information on learning English as a second language.</p>
  </div>
</section>
<div class="container">
  <div class="row">
    <div class="col">
      <p id="notice"><%= notice %></p>
      <table class="table table-stripe text-center">
        <thead class="thead-dark">
          <tr>
            <th>Vocabulary</th>
            <th>User</th>
            <th>Detail Vocabulary</th>
          </tr>
        </thead>
        <tbody>
          <% @posts.each do |post| %>
          <tr>
            <td><%= post.vocabulary %></td>
            <td><%= post.user.username_def %></td>
            <td><%= link_to 'Show', admin_post_path(post),:class => "btn btn-info mr-2" %></td>

          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
<!-- ---------- -->